/*
	* get first n characters of string on javascript array (will find the link)

	* Task
	- Write a function that takes an array (a) as argument
	- Extract the first 3 elements of a
	- Return the resulting array
*/

function main(a) {
	return a.slice(0, 3)
}
